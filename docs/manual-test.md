# Manual test gate

Acceptance criteria that cannot be automated live here, each owned by the ticket
that introduced it. A ticket may be `done` while its entry is `unverified`, but
the entry must exist so the gap is tracked (see `docs/workflow.md` → Manual
gate).

Status: `unverified` | `passed` | `failed`.

| Id | Ticket | Check | Status |
|---|---|---|---|
| MT-001 | 060 | Place two single chests and one double, fill them, name one with `/chest-edit`, select the region with FAWE, run `/uidesigner save`, and confirm the JSON matches `docs/data-format.md` (double counts once, 6 rows; names and slots correct). | unverified |
| MT-002 | 050 | Name a chest, restart the server, and confirm the name survives and appears in a subsequent export. | unverified |
| MT-003 | 040 | With a real (non-MockBukkit) server, confirm the `DoubleChest` holder path merges both halves across all four orientations, not just the geometry fallback. | unverified |
| MT-004 | 030 | Confirm a real FAWE selection scans the expected chests and that a large selection does not stall the server noticeably. | unverified |
| MT-005 | 080 | Cross-world stale selection (not automatable: needs a live FAWE session): select a region in world A, teleport to world B without re-selecting, run `/uidesigner save`, and confirm it reports no selection instead of scanning B at A's coordinates. | unverified |
| MT-006 | 080 | Dev-server end-to-end after the rooster-region swap (not automatable: needs a live FAWE session and Paper): `just run`, place/fill/name chests, select the region with FAWE, run `/uidesigner save`, and confirm the server boots with the library-backed selection and the JSON matches `docs/data-format.md`. | unverified |
| MT-007 | 100 | Dev-server check after the rooster-commands swap (not automatable: needs a live CommandAPI/Brigadier tree): confirm both commands behave as before — `/chest-edit <name>` names, bare `/chest-edit` sends usage (and is a silent no-op from console), `/uidesigner` and `uid` alias respond, suggestions offer the subcommands, and no permission checks exist. | unverified |
| MT-008 | 110 | Export on a non-POSIX filesystem (not automatable on the Linux dev box): run `/uidesigner save` with the output file on a filesystem without POSIX support (e.g. FAT/exFAT mount or WSL `/mnt/c`), and confirm the export succeeds (temp file keeps default permissions) instead of throwing `UnsupportedOperationException`. | unverified |
| MT-009 | 130, 150 | Live-server suggestion check after `clear` was removed (150): `just run`, then type `/chest-edit ` and `/chest-edit cl` and confirm `clear` is not suggested, and that `/uidesigner`/`uid` subcommand suggestions still appear. | unverified |
| MT-010 | 120 | Live-server scan parity after the rooster-region enumeration swap: `just run`, place a real double chest across a chunk border plus a chest in an adjacent unloaded chunk, select the region with FAWE, run `/uidesigner save`, and confirm each chest block is enumerated exactly once with its own cloned 27-slot contents (the double chest's halves stay separate until the grouper merges them). | unverified |
| MT-011 | 140 | Clipped double chest fails closed (not automatable: needs a real FAWE selection and a linked double chest): `just run`, place and fill a real double chest, select only one half (or a region clipping it), run `/uidesigner save`, and confirm no file is written and the error names both halves and the fix hint; then expand the selection to include both halves and confirm one 6-row chest exports. | unverified |
| MT-012 | 150 | Required unique names on a live FAWE selection (not automatable: the save path needs a real selection): `just run`, leave a chest unnamed, run `/uidesigner save`, and confirm no file is written and the error names that chest's position; name two chests `Shop` and `  shop  `, save, and confirm no file is written and the error names the name and both positions; then rename one and confirm every name in the written JSON. Also confirm `/chest-edit clear` names the chest `clear` and a blank argument shows usage without changing an existing name. | unverified |
| MT-013 | 160 | Real double and single scaffolding (not automatable: MockBukkit cannot form a linked `DoubleChest` and its `getTargetBlockExact`/`getFacing` throw): `just run`, put a design file with a 3-row and a 6-row entry in the plugin data folder, aim at open air (no targeted block) so the fallback fires, run `/uidesigner scaffold`, and open each chest: confirm the row starts in the block directly in front of your feet at the anchor's Y and runs across your view, the 3-row is a single chest with its name, the 6-row is one real linked double (54 slots) with its name, and both are empty. | unverified |
| MT-014 | 160 | Scaffold round trip (not automatable: needs a real FAWE selection and a live world): after MT-013, select the placed chests with FAWE and run `/uidesigner save`, then confirm the written JSON reproduces the same names and rows with empty contents. | unverified |
| MT-015 | 160 | Scaffold anchor/obstruction on a live server (not automatable: needs real block targeting, player bounding box, and main-thread placement): run `/uidesigner scaffold` while looking at a non-replaceable block or standing on a target and confirm nothing is placed and the error names the first blocked position; run it with a missing path and with a malformed file and confirm each is rejected before anything is placed (the missing/malformed paths are also unit-tested). | unverified |
| MT-016 | 170 | Status/sync over a placed copy (not automatable: needs a real FAWE selection and a live world): `just run`, save a design with one named chest, scaffold it, fill the selection's chest by running `/uidesigner sync`, then select the chests with FAWE and run `/uidesigner status` and confirm every chest is reported in-sync; note that after `scaffold` alone the empty chests correctly report as differing until `sync` fills them. | unverified |
| MT-017 | 170 | Edit-then-sync restore (not automatable: needs a real FAWE selection and main-thread inventory writes): after MT-016, edit a world chest in-game (remove an item and rename its casing) and run `/uidesigner status`; confirm it is reported `updated`, run `/uidesigner sync`, and confirm the file's item, item name, and chest name are restored and a re-`status` is in-sync. | unverified |
| MT-018 | 170 | Missing and orphan on a live selection (not automatable: needs a real FAWE selection): with a file entry whose chest is not in the selection, confirm `/uidesigner status` reports it `missing`; add a named world chest absent from the file, confirm `status` reports it `orphan`, and confirm `/uidesigner sync` leaves its name and contents untouched (never deleted). | unverified |
| MT-019 | 170 | Unjoinable chests on a live selection (not automatable: needs a real FAWE selection): leave one selected chest unnamed and place two chests named `Shop` and `  shop  `, then run `/uidesigner status` and confirm all three are listed `unjoinable` and none is matched, while a `Shop` file entry is reported `missing` rather than applied to either duplicate. | unverified |
| MT-020 | 170 | `status` never mutates (not automatable: needs a live world and selection): with a drifted placed copy, run `/uidesigner status` and confirm the chest names, inventory contents, and block layout are byte-for-byte unchanged afterwards (no rename, no inventory write, no chunk load beyond the capture scan). | unverified |
| MT-021 | 170 | Structure drift is reported, not repaired (not automatable: MockBukkit cannot form a real linked double): place a 6-row file entry against a single 3-row world chest (or vice versa), run `/uidesigner status` and confirm it reports the rows/structure drift; run `/uidesigner sync` and confirm the chest's name is written, its contents are left untouched, the message says contents were skipped, and the block is still the same single/double (nothing resized or moved). | unverified |
| MT-022 | 170 | Real double-chest contents write (not automatable: MockBukkit cannot form a real `DoubleChest`): with a real linked double chest and a 6-row file entry, select it with FAWE, run `/uidesigner sync`, and confirm all 54 shared slots are written from the file (both halves) via the canonical position's `Chest.inventory`, then `/uidesigner status` is in-sync. | unverified |

## How to run

```sh
just run      # Paper 26.2 on localhost:25000 with FAWE + UiDesigner
```

Join, perform the check in-game, then update the row's status here and commit.
